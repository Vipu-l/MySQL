# Write your MySQL query statement below
select P.project_id ,ifnull(round(avg(E.experience_years),2),0) as average_years from Project P left join Employee E on P.employee_id = E.employee_id group by P.project_id;